
name: App Service Blue-Green (Slots)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read   # REMOVE id-token: write (only needed for OIDC)

env:
  AZURE_RESOURCE_GROUP: 1-35d212df-playground-sandbox
  AZURE_WEBAPP_NAME: 30122025
  STAGING_SLOT: staging

jobs:
  deploy-and-swap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (Service Principal)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          auth-type: SERVICE_PRINCIPAL
        env:
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}

      - name: Deploy HTML to STAGING slot
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          slot-name: ${{ env.STAGING_SLOT }}
          package: '.'

      - name: Swap STAGING -> PRODUCTION
        uses: azure/cli@v2
        with:
          inlineScript: |
            az webapp deployment slot swap \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --name ${{ env.AZURE_WEBAPP_NAME }} \
              --slot ${{ env.STAGING_SLOT }} \
              --target-slot production
